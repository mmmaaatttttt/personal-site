import styled from "styled-components";
import { rhythm } from "../../utils/typography";

const StyledSliderTitle = styled.p`
  margin-bottom: ${rhythm(0.25)};
  font-size: 80%;
  line-height: 1;
`;

export default StyledSliderTitle;
